"""UI foundation layer -- product-agnostic primitives for LabVIEW automation.

Modules:
    coordinates    -- All pixel constants, window sizes, layout mappings
    window_manager -- WindowManager class for find/focus/dismiss/verify
    input_helpers  -- Click, type, hotkey, clear, screenshot, settle
    detection      -- Screenshot region, OCR, pixel diff, template match
    verification   -- Execute VerificationSpec -> VerificationEvidence
    dropdowns      -- Dropdown open/select/detect/retry, listbox nav
"""
